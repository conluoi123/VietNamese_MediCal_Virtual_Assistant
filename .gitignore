# Model weights — never commit to GitHub; store on Drive / HuggingFace Hub
*.pt
*.bin
*.safetensors

# Dataset files — stored on Google Drive, not in repo
data/

# Secrets
.env

# Jupyter cache
.ipynb_checkpoints/

# Python cache
__pycache__/
*.pyc
*.pyo

# LaTeX build artifacts (keep main.pdf for submission)
report/*.aux
report/*.bbl
report/*.blg
report/*.fdb_latexmk
report/*.fls
report/*.lof
report/*.log
report/*.lot
report/*.out
report/*.synctex
report/*.synctex.gz
report/*.toc

# Checkpoint intermediates
results/*.jsonl

# Misc
wandb/
.DS_Store
.venv/
venv/
